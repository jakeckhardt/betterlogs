

export default async function Users() {
    
    // const res = await fetch("http://localhost:3000/api/get-users", { cache: "no-store"});
    // const data = await res.json();

    return (
        <>
            <p>User</p>
        </>
    );
}
